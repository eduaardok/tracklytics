__pycache__/
*.pyc
.env
venv/
.idea/
.vscode/
node_modules/
*.log
.DS_Store
.venv/
parquet_stage/
frontend/dist/
# Cache de portadas resueltas (RF-EXP-009) — regenerable, crece con cada
# corrida de reload_portadas/backfill_portadas; no es código fuente
# (S14-P1, ver docs/BITACORA_S14.md).
etl/gold/portadas_cache.json
.impeccable/

# Comprobantes de estudiante subidos por usuarios reales (P2, S16) — datos
# de usuario, no código fuente; el bind mount `./api:/app` los persiste al
# host pero no deben terminar en el repo.
api/uploads/
.env
__pycache__/
*.pyc
# Cache incremental de tsc -b (artefacto local)
frontend/tsconfig.tsbuildinfo
