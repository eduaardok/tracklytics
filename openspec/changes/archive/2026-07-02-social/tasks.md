## 1. ClickHouse: tablas nuevas

- [x] 1.1 Agregar a `init_clickhouse.py` (idempotente, `CREATE TABLE IF NOT EXISTS`) `BRIDGE_SEGUIMIENTO_ARTISTA` (`ENGINE = MergeTree() ORDER BY (usuario_id, artista_id)`; campos: `usuario_id String`, `artista_id UInt32`, `fecha_inicio DateTime`, `activo UInt8 DEFAULT 1`) (design.md, "`BRIDGE_SEGUIMIENTO_ARTISTA`: soft-delete vía `activo`").
- [x] 1.2 Agregar `DIM_TIPO_INTERACCION_SOCIAL` (`ENGINE = MergeTree() ORDER BY tipo_interaccion_id`; campos: `tipo_interaccion_id UInt16`, `nombre String`, `descripcion String`), sembrada una vez con las 5 filas fijas (`comentario_raiz`, `comentario_respuesta`, `compartir_track`, `compartir_playlist`, `compartir_perfil_artista`), mismo patrón `if _count(...) == 0` que `DIM_ESTADO_REVISION` en `etl/gold/loader.py` (design.md, "`DIM_TIPO_INTERACCION_SOCIAL`: dimensión compartida").
- [x] 1.3 Agregar `FACT_COMENTARIO` (`ENGINE = MergeTree() ORDER BY (fact_id_track, fecha_creacion)`; campos: `fact_id UInt64`, `usuario_id String`, `fact_id_track UInt64`, `tipo_interaccion_id UInt16`, `comentario_padre_id Nullable(UInt64)`, `contenido String`, `fecha_creacion DateTime`, `estado_moderacion Enum8('visible'=1,'oculto'=2,'eliminado'=3) DEFAULT 'visible'`, `moderado_por Nullable(String)`, `fecha_moderacion Nullable(DateTime)`).
- [x] 1.4 Agregar `FACT_COMPARTICION` (`ENGINE = MergeTree() ORDER BY (usuario_id, fecha)`; campos: `fact_id UInt64`, `usuario_id String`, `fact_id_track Nullable(UInt64)`, `artista_id Nullable(UInt32)`, `playlist_id Nullable(String)`, `tipo_interaccion_id UInt16`, `canal Enum8('x'=1,'whatsapp'=2,'copiar_enlace'=3)`, `fecha DateTime`) — exactamente una de `fact_id_track`/`artista_id`/`playlist_id` está poblada por fila según `tipo_interaccion_id`, las otras dos quedan `NULL` (design.md, "`FACT_COMPARTICION`: solo registra intención, sin llamada externa").
- [x] 1.5 Ejecutar `init_clickhouse.py` y verificar que las 4 tablas existen y que `DIM_TIPO_INTERACCION_SOCIAL` tiene exactamente 5 filas.

## 2. Backend: paquete `social` — utilidades y consultas

- [x] 2.1 Crear `api/paquetes/social/__init__.py`.
- [x] 2.2 Crear `api/paquetes/social/queries.py`: consultas de artistas seguidos por usuario (`activo=1`), verificación de seguimiento activo existente, comentarios visibles de un track (excluye `estado_moderacion='eliminado'` propio y de su padre, vía subconsulta/join sobre la misma tabla), cola administrativa de comentarios filtrable por `fact_id_track`/`estado_moderacion`, y existencia de artista (`DIM_ARTISTS`) / track (`FACT_TRACKS`) / comentario padre (`FACT_COMENTARIO`).
- [x] 2.3 Crear `api/paquetes/social/deps.py` si se requiere alguna dependencia propia; reutilizar directamente `core.deps.get_current_user`, `core.deps.require_b2c_user` y `paquetes.seguridad.deps.require_admin` — no reimplementar autorización (design.md, "Autorización: `require_b2c_user` para escritura, `require_admin` solo para moderación").
- [x] 2.4 Implementar en `router.py` (o un módulo auxiliar) la generación de `fact_id UInt64` vía `random.getrandbits(50)` para `FACT_COMENTARIO`/`FACT_COMPARTICION`, sin lock ni `SELECT max()+1` (design.md, "`fact_id` ... `UInt64` aleatorio ... sin lock"). Corregido de 63→50 bits durante el craft de frontend: 63 bits excede `Number.MAX_SAFE_INTEGER` de JS y pierde precisión en el cliente React (design.md, "Corrección aplicada durante el craft de frontend").

## 3. Backend: endpoints de seguimiento (CU-O29/CU-O30/CU-O31)

- [x] 3.1 Implementar `POST /app/v1/social/seguimiento/{artista_id}` (`Depends(require_b2c_user)`): valida que el artista exista en `DIM_ARTISTS` (404 si no), rechaza si ya existe una fila activa para ese par usuario/artista (409), e inserta en `BRIDGE_SEGUIMIENTO_ARTISTA` con `activo=1` y `fecha_inicio=now()` (RF: Seguir a un artista, CU-O29).
- [x] 3.2 Implementar `DELETE /app/v1/social/seguimiento/{artista_id}` (`Depends(require_b2c_user)`): rechaza si no existe una fila activa (404), y de lo contrario ejecuta `ALTER TABLE BRIDGE_SEGUIMIENTO_ARTISTA UPDATE activo = 0 WHERE usuario_id = ... AND artista_id = ... AND activo = 1` (RF: Dejar de seguir a un artista, CU-O30).
- [x] 3.3 Implementar `GET /app/v1/social/seguimiento` (`Depends(get_current_user)`): retorna los artistas con `activo=1` para el usuario autenticado (RF: Consulta de artistas seguidos, CU-O31). No requiere `require_b2c_user` — es de solo lectura sobre los propios datos, pero un Cliente B2B nunca tendrá filas (no puede escribir), así que el resultado siempre es vacío para ese rol sin necesidad de bloquear la lectura explícitamente.

## 4. Backend: endpoints de comentarios (CU-O32/CU-O33)

- [x] 4.1 Implementar `POST /app/v1/social/comentarios` (`Depends(require_b2c_user)`): body con `fact_id_track`, `contenido`, `comentario_padre_id` opcional; valida que el track exista (404 si no) y, si se indica padre, que el comentario padre exista y pertenezca al mismo track (404/422 si no); inserta en `FACT_COMENTARIO` con `tipo_interaccion_id` resuelto según raíz/respuesta y `estado_moderacion='visible'` (RF: Comentar un track, CU-O32).
- [x] 4.2 Implementar `GET /app/v1/social/comentarios/{fact_id_track}` (`Depends(get_current_user)`, sin restricción de rol): retorna los comentarios visibles/ocultos (excluye solo `eliminado`, y excluye comentarios cuyo padre esté `eliminado`) ordenados por `fecha_creacion` (RF: Consulta de comentarios de un track, CU-O33; incluye el escenario de Cliente B2B con acceso de lectura).

## 5. Backend: endpoints de moderación (CU-O34/CU-O35)

- [x] 5.1 Implementar `POST /app/v1/social/admin/comentarios/{fact_id}/moderar` (`Depends(require_admin)`): body con la decisión (`oculto`/`eliminado`); valida que el comentario exista (404 si no); ejecuta `ALTER TABLE FACT_COMENTARIO UPDATE estado_moderacion = ..., moderado_por = ..., fecha_moderacion = now() WHERE fact_id = ...` (RF: Moderación de un comentario, CU-O34). Registrar en `FACT_AUDIT_LOG` vía `audit.record(usuario_id=admin_id, accion="moderacion_comentario", tabla_afectada="FACT_COMENTARIO", antes={...}, despues={...})` (RN-SOC-002).
- [x] 5.2 Implementar `GET /app/v1/social/admin/comentarios` (`Depends(require_admin)`, filtrable por `fact_id_track`/`estado_moderacion`): retorna todos los comentarios con su estado de moderación, sin excluir ninguno (RF: Cola administrativa de comentarios, CU-O35).

## 6. Backend: endpoint de compartir (CU-O36)

- [x] 6.1 Implementar `POST /app/v1/social/comparticiones` (`Depends(require_b2c_user)`): body con `tipo_interaccion_id` (`compartir_track`/`compartir_playlist`/`compartir_perfil_artista`), `canal` (`x`/`whatsapp`/`copiar_enlace`) y el identificador del objeto compartido en su propio campo (`fact_id_track`/`artista_id`/`playlist_id`). La validación de existencia se ramifica según `tipo_interaccion_id`: si es `compartir_track`, valida `fact_id_track` contra `FACT_TRACKS` (404 si no existe); si es `compartir_perfil_artista`, valida `artista_id` contra `DIM_ARTISTS` (404 si no existe, mismo patrón que la tarea 3.1 de seguir a un artista); si es `compartir_playlist`, no valida `playlist_id` (las playlists viven en PocketBase, fuera de este dominio). Inserta en `FACT_COMPARTICION` con solo el campo correspondiente poblado; construye y retorna el texto/enlace correspondiente al canal sin llamar a ningún servicio externo (RF: Compartir contenido, CU-O36).

## 7. Backend: montaje del router

- [x] 7.1 Montar el router de `social` (`prefix="/app/v1/social"`) en `api/main.py` junto al resto de routers.

## 8. Frontend: completar el paquete `social` en `frontend/src/packages/social/`

- [x] 8.1 Definir `types.ts` (Seguimiento, Comentario, Comparticion, y los payloads de request de seguir, comentar/responder, moderar y compartir) reflejando los campos de las 4 tablas nuevas, mismo patrón que `packages/creadores/types.ts`.
- [x] 8.2 Implementar `api/social.api.ts` con llamadas tipadas a `/app/v1/social/*` (mismo patrón que `packages/facturacion/api/facturacion.api.ts`).
- [x] 8.3 **Decisión de arquitectura confirmada (no hay vista de perfil de artista ni de detalle de track en React todavía — `TrackCard.tsx` tiene su click deliberadamente deshabilitado, esa migración es trabajo de `experiencia`, no de `social`):** implementar `pages/ArtistaSocialPage.tsx` en `frontend/src/packages/social/pages/`, ruta `/social/artista/:artistaId`. Contenido mínimo: nombre del artista (fetch puntual, reutilizando el endpoint de detalle que ya expone `catalogo` para `DIM_ARTISTS`, sin duplicar esa consulta) y el botón seguir/dejar de seguir consumiendo `socialApi`. No es la vista de perfil de artista real ni toca el paquete `catalogo` — placeholder funcional hasta que `experiencia` construya el detalle real.
- [x] 8.4 Implementar `pages/TrackSocialPage.tsx` en el mismo paquete, ruta `/social/track/:factId`. Contenido mínimo: nombre del track (fetch puntual equivalente) y la sección de comentarios completa (listado + formulario de comentario raíz + responder + botón compartir del punto 8.5), mostrando un placeholder para comentarios en estado `oculto` en vez de su `contenido` original (design.md, "Riesgo] Un comentario `oculto` sigue siendo legible..."). Tampoco toca `catalogo` ni reactiva el click de `TrackCard`.
- [x] 8.5 Implementar el botón de compartir (track/perfil de artista/playlist) con selector de canal dentro de `TrackSocialPage`/`ArtistaSocialPage` según corresponda, mostrando el enlace/texto retornado por el backend (copiar al portapapeles para `copiar_enlace`).
- [x] 8.6 Implementar `pages/ModeracionSocialPage.tsx`: vista admin-only con la cola de comentarios (filtrable por track/estado) y acciones ocultar/eliminar, mismo patrón que `packages/creadores/pages/RevisionCreadoresPage.tsx`.
- [x] 8.7 Exponer únicamente lo necesario desde `index.ts` (páginas + `socialApi` + tipos), siguiendo la regla de aislamiento ya documentada en el README del stub.
- [x] 8.8 Wiring de rutas: montar `/social` (hub `SeguidosSocialPage`), `/social/artista/:artistaId` y `/social/track/:factId` bajo `AppShell`; montar `/seguridad/social` (`ModeracionSocialPage`) bajo `SeguridadShell` — mismo patrón de path que `/seguridad/creadores`/`/seguridad/facturacion`, en vez del `/social/admin/moderacion` planteado originalmente. Agregar "Social" a la barra lateral admin (junto a permisos/auditoría/creadores) y a la nav principal de `AppShell`; el hub `SeguidosSocialPage` sirve como la entrada temporal de navegación para las dos páginas B2C (lista de artistas seguidos con enlace a `ArtistaSocialPage`, más un campo para saltar a `TrackSocialPage` por `fact_id`) ya que no hay enlace natural desde `TrackCard` todavía — documentado en 8.9.
- [x] 8.9 Actualizar el README del paquete quitando la marca de "stub" y documentando: (a) la misma brecha conocida de sesión autenticada en React ya anotada en `facturacion`/`creadores` (si sigue vigente al momento de esta ronda); (b) que `ArtistaSocialPage`/`TrackSocialPage` son vistas mínimas temporales sin entrada natural de navegación desde `catalogo` — la navegación real (desde `TrackCard`/un perfil de artista de verdad) es responsabilidad de la capability `experiencia`, que reactivará el click de `TrackCard` y podrá reincorporar estos componentes de seguir/comentar/compartir dentro de esa vista real.

## 9. Verificación end-to-end

- [x] 9.1 Verificar (curl, token de usuario de prueba) seguir a un artista existente, y que un segundo intento sobre el mismo artista es rechazado (409).
- [x] 9.2 Verificar dejar de seguir y que la fila queda con `activo=0` sin eliminarse (consulta directa a ClickHouse).
- [x] 9.3 Verificar `GET /app/v1/social/seguimiento` retorna solo los artistas activos del usuario autenticado.
- [x] 9.4 Verificar comentar un track existente (comentario raíz) y responder a ese comentario (comentario_padre_id).
- [x] 9.5 Verificar que comentar un track inexistente devuelve 404, y que responder con un `comentario_padre_id` de otro track es rechazado.
- [x] 9.6 Verificar (curl, token admin) ocultar un comentario y confirmar que sigue apareciendo en `GET /app/v1/social/comentarios/{fact_id_track}` con `estado_moderacion='oculto'`.
- [x] 9.7 Verificar eliminar un comentario y confirmar que desaparece de `GET /app/v1/social/comentarios/{fact_id_track}`, junto con cualquier respuesta que tuviera como padre a ese comentario.
- [x] 9.8 Verificar que un usuario sin rol admin recibe 403 al intentar moderar un comentario o consultar `GET /app/v1/social/admin/comentarios`.
- [x] 9.9 Verificar en `FACT_AUDIT_LOG` (vía `GET /app/v1/seguridad/auditoria` como admin) que cada moderación quedó registrada con el estado antes/después correcto.
- [x] 9.10 Verificar (token de Cliente B2B/analyst) que seguir, comentar y compartir devuelven 403, y que `GET /app/v1/social/comentarios/{fact_id_track}` sí funciona para ese rol.
- [x] 9.11 Verificar compartir un track existente, un perfil de artista existente y una playlist (`playlist_id` arbitrario, sin validar) por los tres canales soportados, que un canal no soportado es rechazado, y que compartir un track o un perfil de artista inexistente devuelve 404.
- [x] 9.12 Verificar con `npx tsc --noEmit` que el paquete `social` y los archivos de routing/shell modificados no introducen errores de tipos.
