## 1. Modelo de datos (ClickHouse)

- [x] 1.1 Agregar `FACT_REPRODUCCION_EVENTO` a `init_clickhouse.py` (fact_id UInt64 PK, usuario_id, fact_id_track, dispositivo_id, sesion_id, porcentaje_completado Float32, fecha DateTime; MergeTree() ORDER BY (fact_id_track, fecha)).
- [x] 1.2 Agregar `FACT_IMPRESION_RECOMENDACION` (fact_id, usuario_id, fact_id_track, algoritmo String, fue_reproducido UInt8, fecha; MergeTree() ORDER BY (usuario_id, fecha)).
- [x] 1.3 Agregar `FACT_TICKET_SOPORTE` (fact_id, usuario_id, asunto, descripcion, estado Enum8('abierto'=1,'en_proceso'=2,'resuelto'=3,'cerrado'=4), fecha_creacion, fecha_resolucion Nullable(DateTime); MergeTree() ORDER BY (usuario_id, fecha_creacion)).
- [x] 1.4 Agregar `FACT_AB_TEST_EXPOSICION` (fact_id, usuario_id, experimento, variante, fecha; MergeTree() ORDER BY (experimento, usuario_id)).
- [x] 1.5 Agregar `BRIDGE_TRACK_PLAYLIST_USUARIO` (fact_id_track, usuario_id, playlist_id String, fecha_agregado; MergeTree() ORDER BY (fact_id_track, usuario_id)).
- [x] 1.6 Agregar `BRIDGE_SUSCRIPTOR_FAMILIA` (suscripcion_id, usuario_id, es_titular UInt8, fecha_union; MergeTree() ORDER BY suscripcion_id).
- [x] 1.7 Agregar columnas `imagen_url Nullable(String)` a `DIM_ARTISTS` y `DIM_ALBUMS` (aditivo, sin `ALTER ... DROP`).
- [x] 1.8 Aplicar el DDL contra ClickHouse en Docker y verificar con `curl`/`clickhouse-client` que las 6 tablas y las 2 columnas nuevas existen con el esquema esperado.

## 2. Backend — paquete `api/paquetes/experiencia/`

- [x] 2.1 Crear `deps.py` reusando `require_admin` (de `seguridad`) y `require_b2c_user`/`get_current_user` (de `core.deps`) — sin definir un mecanismo de autorización nuevo.
- [x] 2.2 `queries.py`: queries de inserción/lectura para las 6 tablas nuevas, siguiendo el patrón de parámetros nombrados ya usado en el resto del proyecto.
- [x] 2.3 Endpoint interno (no HTTP propio): función reusable de registro de evento de reproducción enriquecido, invocada desde el endpoint de reproducción ya existente en `biblioteca` (RF-EXP-001) — generar `fact_id` con `random.getrandbits(50)`, mismo patrón que `social`. Extra necesario no listado originalmente: `biblioteca/router.py::add_historial` ahora acepta un body opcional (`dispositivo_id`, `porcentaje_completado`, `impresion_id`) y `app/js/history.js::addToHistory` envía `dispositivo_id` (`getDeviceId()`) — sin esto el evento enriquecido nunca se habría disparado desde el cliente real. `sesion_id` se resuelve reusando `paquetes/seguridad/queries.py::SESION_ABIERTA_POR_DISPOSITIVO`.
- [x] 2.4 Endpoint interno de registro de impresión de recomendación y actualización de `fue_reproducido` (RF-EXP-002/003), invocado desde donde el frontend solicite recomendaciones y desde el flujo de reproducción existente. Extra necesario no listado originalmente: no existía ningún endpoint que sirviera recomendaciones — se agregó `GET /app/v1/experiencia/recomendaciones` (mismo género que favoritos, fallback a popularidad global) que registra la impresión de cada track devuelto y devuelve su `impresion_id`; `add_historial` lo reenvía a `marcar_impresion_reproducida` cuando el cliente reproduce esa recomendación.
- [x] 2.5 `POST /app/v1/experiencia/tickets` — Usuario B2C crea un ticket (RF-EXP-004, CU-O45).
- [x] 2.6 `GET /app/v1/experiencia/tickets` — Usuario B2C consulta sus propios tickets; con rol admin, consulta todos filtrables por estado (RF-EXP-004/005, CU-O46/CU-O47).
- [x] 2.7 `PUT /app/v1/experiencia/tickets/{id}` — admin actualiza estado, registra `fecha_resolucion` si pasa a resuelto (RF-EXP-005, CU-O48).
- [x] 2.8 `POST /app/v1/experiencia/playlists/sincronizar` — admin fuerza resincronización del bridge de playlists fuera del ciclo semanal (RF-EXP-006, CU-O49) — dispara el DAG `playlists_sync` en Airflow (sección 3), mismo patrón HTTP que `gestion_datos/router.py::etl_trigger`.
- [x] 2.9 `GET /app/v1/experiencia/playlists/top-tracks` — analyst o admin consulta tracks más agregados a playlists desde `BRIDGE_TRACK_PLAYLIST_USUARIO` (RF-EXP-007, CU-O50).
- [x] 2.10 `POST /app/v1/experiencia/familia/titular` — admin designa titular de plan familiar, validando suscripción activa, que su `tipo_plan` sea `premium` (cruzando la suscripción en PocketBase vía `pb_client`/`suscripciones` contra `PLANES` de `planes.py` — ClickHouse no valida tipo de plan, no existe esa columna) y que no sea ya titular/miembro de otro plan (RF-EXP-008, CU-O51, design.md "elegibilidad de plan"). Verificado con curl real: rechazo 403 al intentar designar un usuario con plan B2B `pro` como titular.
- [x] 2.11 `POST /app/v1/experiencia/familia/{suscripcion_id}/miembros` — admin agrega miembro, validando el límite de 5 personas y que no pertenezca ya a otro plan familiar (RF-EXP-008, CU-O52). Extra no listado originalmente: se agregó `GET /app/v1/experiencia/familia/{suscripcion_id}` (admin) para listar miembros actuales + conteo contra el límite — necesario para que el frontend (5.5) pueda mostrarlo.
- [x] 2.12 `DELETE /app/v1/experiencia/familia/{suscripcion_id}/miembros/{usuario_id}` — admin quita miembro (CU-O53, completa el CRUD de la relación).
- [x] 2.13 `audit.record` (de `seguridad`) en cada operación admin de tickets y de plan familiar (actualizar estado, crear titular, agregar/quitar miembro).
- [x] 2.14 Montar el router nuevo en `api/main.py`, con el mismo prefijo `/app/v1/experiencia` que ya usan las demás capabilities recientes (sin necesitar cambios de proxy en `vite.config.ts`/`nginx.conf`, que ya cubren `/app/v1/*`).

## 3. ETL — sincronización de playlists y resolución de portadas

- [x] 3.1 Job Python (`etl/`) que lee `playlists`/`playlist_tracks` de PocketBase y hace upsert en `BRIDGE_TRACK_PLAYLIST_USUARIO`, respetando RT-01 (Parquet como stage intermedio, igual que el resto del pipeline). Implementado como `etl/gold/playlists_sync.py::run_playlists_sync` — full refresh (TRUNCATE + reload) en cada corrida, no upsert incremental fila a fila (volumen bajo, ver comentario en el archivo).
- [x] 3.2 Integrar ese job a la corrida semanal de ingesta ya existente (mismo `week_number`/`ETL_BATCH_CONTROL`), y exponerlo también como acción invocable desde `POST /app/v1/experiencia/playlists/sincronizar` (tarea 2.8). Implementado como DAG independiente `etl/dags/playlists_sync_dag.py` (`playlists_sync`, `schedule_interval=None`), mismo patrón que `engagement_dag.py` — mismo ritmo semanal por disparo manual/API, sin acoplarse a `tracklytics_etl` para no forzar una re-ingesta completa del catálogo en cada resincronización. Verificado con un DAG run real vía el endpoint on-demand: 11 filas sincronizadas, `GET /playlists/top-tracks` las refleja correctamente.
- [x] 3.3 Extensión del job de carga: para tracks del catálogo licenciado base, resolver portada en un directorio musical externo público y guardar la URL en `DIM_ARTISTS.imagen_url`/`DIM_ALBUMS.imagen_url`; sin resultado o fuera del catálogo base, dejar el campo nulo (el fallback visual lo resuelve el frontend, no el ETL). Implementado en `etl/gold/portada.py` (iTunes Search API, sin credencial) como `task_portada` nuevo en `tracklytics_etl`, entre `task_gold` y `task_synthetic`. Verificado con `airflow tasks test`: portadas reales persistidas en `DIM_ALBUMS.imagen_url`.
- [x] 3.4 Manejo de error/timeout del directorio externo en el ETL: un fallo de red no debe interrumpir la carga completa del batch — se registra y se continúa. Verificado con el propio rate-limit real de iTunes (429) durante la corrida de prueba: cada fallo se logueó y el batch completo terminó en estado `success`.

## 4. Frontend — extender componentes ya existentes de `catalogo`/`shared`

- [x] 4.1 Extender `frontend/src/shared/context/PlayerContext.tsx` con una fuente de audio real (no reconstruir el contexto existente) — reemplazar el avance de posición simulado por el estado real del reproductor embebido cuando hay una fuente disponible. Implementado con la YouTube IFrame Player API (`playerVars.listType = 'search'`, sin API key) — ya anticipado en un comentario preexistente del archivo. Progreso real vía `onStateChange`/`getCurrentTime`, no un timer simulado.
- [x] 4.2 Extender `frontend/src/shared/components/PlayerBar.tsx` con el reproductor embebido real (búsqueda por texto desde el cliente); estado deshabilitado o "no disponible" cuando no hay resultado o no hay conexión (RF-EXP-010). `playbackUnavailable` (offline al hacer play, o `onError` del IFrame) deshabilita el botón de play y reemplaza la barra de progreso por "No disponible".
- [x] 4.3 Reactivar el click de reproducción en `frontend/src/packages/catalogo/components/TrackCard.tsx` para usar la fuente real, manteniendo el fallback ya decidido en el diseño. `TrackCard` ya llamaba a `play()`/`registrarReproduccion()`; no necesitó cambios propios — se extendió `bibliotecaApi.registrarReproduccion` para enviar `dispositivo_id` (`getDeviceId()` de `shared/lib/session.ts`, RF-EXP-001).
- [x] 4.4 Mostrar `imagen_url` en `TrackCard.tsx` y en las páginas de detalle de artista/álbum de `catalogo`; reemplazo visual local (SVG, tokens de diseño ya establecidos) cuando `imagen_url` sea nulo — cero llamada externa en ese camino. Se agregó `imagen_url` a las queries de `api/paquetes/catalogo/queries.py` que alimentan estas vistas (columna nueva, aditiva, sin cambiar el resto de sus campos) y un componente compartido `shared/components/AlbumArt.tsx` con fallback a `<img onError>`.
- [x] 4.5 Actualizar `openspec/specs/catalogo/spec.md`, sección "Fuera de alcance": quitar o corregir la línea de reproducción de audio real (ya no aplica) y la referencia a que las recomendaciones están cubiertas en `analitica` (viven en `experiencia`) — edición directa de housekeeping, no delta spec formal (ver proposal.md).

## 5. Frontend — nuevo paquete `frontend/src/packages/experiencia/`

- [x] 5.1 `types.ts`: tipos para ticket de soporte, plan familiar, resultado de sincronización de playlists y top-tracks.
- [x] 5.2 `api/experiencia.api.ts`: cliente tipado sobre `/app/v1/experiencia/*` reusando `apiClient`/sesión ya establecidos (mismo patrón que el resto de paquetes recientes).
- [x] 5.3 `SoportePage.tsx` (Usuario B2C): crear ticket, listar los propios tickets con su estado. Ruta `/soporte` (AppShell, bajo el menú "Más").
- [x] 5.4 Página admin de tickets (bajo `/seguridad`, mismo patrón que `partners`/`ingesta` del bloque anterior — el shell ya está envuelto en `RequireAuth roles={['admin']}`, sin duplicar el guard por ruta): listar todos los tickets filtrables por estado, actualizar estado. `TicketsAdminPage.tsx`, ruta `/seguridad/soporte`.
- [x] 5.5 Página admin de plan familiar (bajo `/seguridad`): crear titular, agregar/quitar miembro, mostrar el conteo actual contra el límite de 5. `FamiliaAdminPage.tsx`, ruta `/seguridad/familia`.
- [x] 5.6 Página o sección (bajo `/seguridad` o `/analitica`, a decidir contra el patrón ya establecido para consultas tácticas) para "tracks más agregados a playlists" (CU-O50) y botón de resincronización manual (CU-O49) — Cliente B2B y admin respectivamente. Decisión: bajo `/analitica` (mismo shell/gating — `RequireSuscripcionActiva` — que el resto de consultas tácticas de Cliente B2B). `TopTracksPlaylistsPage.tsx`, ruta `/analitica/playlists-top`; botón "Resincronizar ahora" condicionado a `getRole() === 'admin'`.
- [x] 5.7 Registrar rutas nuevas en `frontend/src/app/router.tsx` y nav en el shell correspondiente, siguiendo el mismo patrón de los bloques anteriores.
- [x] 5.8 `index.ts`: exportar únicamente la interfaz pública del paquete, respetando la regla de aislamiento ya establecida en el resto de paquetes.

## 6. Verificación

- [x] 6.1 Backend: verificar con `curl` real contra la API en Docker cada endpoint nuevo (tickets, plan familiar, sincronización de playlists, top-tracks), incluyendo los casos de error (límite de plan familiar, usuario ya en otro plan, rol incorrecto, ticket/plan inexistente). Verificado extensamente contra usuarios reales (user/admin/analyst) creados para la prueba: creación/consulta/actualización de tickets (incl. 422/403/404), titular+miembros de plan familiar (incl. 403 por plan no-premium, 409 duplicado, 403 por límite de 5, 404 al quitar inexistente), recomendaciones con impresión+reproducción real, y reproducción enriquecida con `sesion_id` resuelto desde una sesión real abierta por login.
- [x] 6.2 ETL: correr el job de sincronización de playlists y el de resolución de portadas contra datos reales de Docker, confirmar los registros resultantes en ClickHouse. `playlists_sync` corrido vía Airflow real (disparado desde el endpoint on-demand): 11 filas sincronizadas, reflejadas correctamente en `GET /playlists/top-tracks`. `task_portada` corrido vía `airflow tasks test` contra `tracklytics_etl`: portadas reales persistidas en `DIM_ALBUMS.imagen_url` (verificado con fallos 429 de iTunes absorbidos sin interrumpir el batch).
- [x] 6.3 Frontend: `tsc --noEmit` y `npm run build` limpios; smoke test con Playwright de las páginas nuevas (rutas, guards, sin errores de consola) y verificación visual de portada real / fallback y reproducción real / estado no disponible. `tsc --noEmit` y `npm run build` limpios (los únicos errores de `tsc` son preexistentes en `EngagementPage.tsx`, no tocado en este cambio). Playwright (`chromium` headless) contra `npm run dev`: creación de ticket B2C, consulta+actualización admin, plan familiar admin, `playlists-top` con/sin botón de resync según rol, y click de reproducción real en catálogo — 0 errores de consola en la corrida final, ambos estados de RF-EXP-010 observados (audio real reproduciendo con duración correcta en una corrida, "No disponible" en otra). Este ejercicio encontró y corrigió un bug real: `TOP_TRACKS_PLAYLIST` devolvía `fact_id` duplicado cuando `FACT_TRACKS` tiene dos filas con el mismo `fact_id` (dato preexistente del dataset) — corregido agrupando únicamente por `fact_id_track`.
- [x] 6.4 Confirmar que `FACT_ENGAGEMENT_USUARIO` y el score de engagement en `analitica` siguen funcionando sin cambios tras agregar `FACT_REPRODUCCION_EVENTO`. Verificado: `FACT_ENGAGEMENT_USUARIO` sigue insertando (reproducciones nuevas contadas), `GET /app/v1/analitica/engagement?fact_id=...` y `/desempeno-relativo` responden con los mismos scores esperados.
