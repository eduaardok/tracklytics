## 1. ClickHouse: tablas nuevas

- [x] 1.1 Agregar a `init_clickhouse.py` (idempotente, `CREATE TABLE IF NOT EXISTS`) `DIM_PAIS` (`ENGINE = MergeTree() ORDER BY pais_id`; campos: `pais_id UInt16`, `nombre String`, `codigo_iso String`) (design.md, tabla `DIM_PAIS`).
- [x] 1.2 Agregar `DIM_SELLO_DISCOGRAFICO` (`ENGINE = MergeTree() ORDER BY sello_id`; campos: `sello_id UInt32`, `nombre String`) (design.md, Decisión 1).
- [x] 1.3 Agregar `DIM_LICENCIA` (`ENGINE = MergeTree() ORDER BY (sello_id, pais_id)`; campos: `licencia_id UInt32`, `sello_id UInt32`, `pais_id UInt16`, `fecha_inicio Date`, `fecha_fin Nullable(Date)`, `estado Enum8('activa'=1,'vencida'=2,'suspendida'=3) DEFAULT 'activa'`) (design.md, Decisión 2).
- [x] 1.4 Agregar `DIM_TIPO_RESTRICCION` (`ENGINE = MergeTree() ORDER BY tipo_restriccion_id`; campos: `tipo_restriccion_id UInt16`, `nombre String`, `descripcion String`), sembrada una vez con 3 filas fijas (`no_disponible`, `solo_preview`, `geo_bloqueado`), mismo patrón `if count == 0` que `DIM_ESTADO_REVISION` en `init_clickhouse.py` (líneas ~595-609).
- [x] 1.5 Agregar `DIM_CANAL_DISTRIBUCION` (`ENGINE = MergeTree() ORDER BY canal_id`; campos: `canal_id UInt16`, `nombre String`), sembrada una vez con 3 filas fijas (`streaming`, `descarga`, `sync_licensing`), mismo patrón de seed condicional.
- [x] 1.6 Agregar `BRIDGE_RESTRICCION_TRACK` (`ENGINE = MergeTree() ORDER BY (fact_id_track, pais_id, canal_id)`; campos: `fact_id_track UInt64`, `pais_id UInt16`, `canal_id UInt16`, `tipo_restriccion_id UInt16`, `fecha_inicio DateTime`, `activo UInt8 DEFAULT 1`), mismo patrón soft-delete que `BRIDGE_SEGUIMIENTO_ARTISTA` (design.md, "Restricción ... independiente de la licencia").
- [x] 1.7 Agregar `FACT_RESTRICCION_REPRODUCCION` (`ENGINE = MergeTree() ORDER BY (fact_id_track, fecha)`; campos: `fact_id UInt64`, `usuario_id String`, `fact_id_track UInt64`, `pais_id UInt16`, `tipo_restriccion_id UInt16`, `fecha DateTime`) (design.md, Decisión 6).
- [x] 1.8 Ejecutar `init_clickhouse.py` y verificar que las 7 tablas existen, y que `DIM_TIPO_RESTRICCION` tiene exactamente 3 filas y `DIM_CANAL_DISTRIBUCION` exactamente 3 filas.
- [x] 1.9 Poblar `DIM_PAIS` con un catálogo fijo inicial de países (mínimo los que ya aparecen como valores distintos en `DIM_USUARIO.pais`, más los mercados principales del dataset), con `codigo_iso` de 2 letras — script Python de una sola ejecución, no vía frontend.

## 2. Migración BREAKING: `record_label`/`label` → `sello_id` (design.md, Decisión 1)

- [x] 2.1 Script Python de una sola ejecución (`scripts/migrar_sellos.py` o equivalente): `SELECT DISTINCT record_label FROM DIM_ARTISTS WHERE record_label != ''` y `SELECT DISTINCT label FROM DIM_ALBUMS WHERE label != ''`, unir ambos conjuntos de nombres distintos.
- [x] 2.2 Poblar `DIM_SELLO_DISCOGRAFICO` con esos nombres distintos, `sello_id` autoincremental asignado en Python (script de un solo uso, no hay concurrencia que proteger aquí).
- [x] 2.3 `ALTER TABLE DIM_ARTISTS ADD COLUMN IF NOT EXISTS sello_id UInt32 DEFAULT 0` y `ALTER TABLE DIM_ALBUMS ADD COLUMN IF NOT EXISTS sello_id UInt32 DEFAULT 0` en `init_clickhouse.py`.
- [x] 2.4 Ejecutar `ALTER TABLE DIM_ARTISTS UPDATE sello_id = ... WHERE record_label = ...` (mutación) por cada sello distinto, y el equivalente en `DIM_ALBUMS UPDATE sello_id = ... WHERE label = ...`.
- [x] 2.5 Verificar cobertura: contar filas con `sello_id = 0` y `record_label != ''` / `label != ''` en `DIM_ARTISTS`/`DIM_ALBUMS` — debe ser 0 antes de continuar al siguiente paso.
- [x] 2.6 `ALTER TABLE DIM_ARTISTS DROP COLUMN record_label` y `ALTER TABLE DIM_ALBUMS DROP COLUMN label` — solo después de verificar 2.5 (design.md, Migration Plan, paso 4; Riesgo de irreversibilidad).
- [x] 2.7 Actualizar `init_clickhouse.py` (definición de `DIM_ARTISTS`/`DIM_ALBUMS`, líneas ~68-91): quitar `record_label`/`label`, agregar `sello_id UInt32`, para que una instalación nueva desde cero cree el esquema final directamente sin pasar por la migración.
- [x] 2.8 Migrar `api/paquetes/catalogo/queries.py:138,147` (proyección/GROUP BY de `a.record_label`) a un `JOIN DIM_SELLO_DISCOGRAFICO s ON a.sello_id = s.sello_id`, proyectando `s.nombre AS sello_nombre`.
- [x] 2.9 Migrar `api/paquetes/partners/queries.py:65,69` (mismo patrón `a.record_label`) al mismo join por `sello_id`.
- [x] 2.10 Actualizar `etl/gold/loader.py:180,204` (`column_names` de la carga de `DIM_ALBUMS`/`DIM_ARTISTS`) para escribir `sello_id` en vez de `label`/`record_label` en las cargas semanales de datos nuevos — el ETL resuelve `sello_id` contra `DIM_SELLO_DISCOGRAFICO` por nombre, creando el sello si no existe todavía.
- [x] 2.11 Actualizar `api/paquetes/creadores/promocion.py:54,70` (promoción de `STG_ARTIST_UPLOADS` a `DIM_ARTISTS`/`DIM_ALBUMS`) para escribir `sello_id = 0` (sin sello asignado) en vez de `record_label`/`label`, ya que un artista/álbum recién promovido desde una subida de usuario no tiene sello discográfico todavía — se asigna después vía RF-DIS-002.

## 3. Backend: paquete `distribucion` — utilidades y consultas

- [x] 3.1 Crear `api/paquetes/distribucion/__init__.py`.
- [x] 3.2 Crear `api/paquetes/distribucion/queries.py`: CRUD de sellos, asignación de `sello_id` a artista/álbum, alta y consulta de licencias por sello/país (incluyendo vencidas), alta/consulta/desactivación de restricciones, verificación de restricción activa por `(fact_id_track, pais_id, canal_id)`, resolución de `sello_id` de un track (precedencia álbum > artista, design.md Decisión 2), y resolución de `pais_id` desde `DIM_USUARIO.pais` por comparación case-insensitive contra `DIM_PAIS.nombre`.
- [x] 3.3 Crear `api/paquetes/distribucion/router.py` con `prefix="/app/v1/distribucion"`, reutilizando directamente `require_admin` (`paquetes.seguridad.deps`), `audit.record` (`paquetes.seguridad.audit`) y `require_b2c_user`/`get_current_user` (`core.deps`) — no reimplementar autorización ni auditoría (design.md, Context).
- [x] 3.4 Implementar en `router.py` la generación de `fact_id UInt64` para `FACT_RESTRICCION_REPRODUCCION` vía `random.getrandbits(50)`, mismo patrón ya corregido en `api/paquetes/social/router.py:59` — no usar 63 bits.

## 4. Backend: endpoints de administración de sellos (CU-O37/CU-O38)

- [x] 4.1 Implementar `POST /app/v1/distribucion/sellos` (`Depends(require_admin)`): crea un sello con `nombre` no vacío; registra en `FACT_AUDIT_LOG` vía `audit.record` (RF-DIS-001, CU-O37).
- [x] 4.2 Implementar `PUT /app/v1/distribucion/sellos/{sello_id}` (`Depends(require_admin)`): edita el nombre de un sello existente (404 si no existe); registra estado antes/después en `FACT_AUDIT_LOG` (RF-DIS-001, CU-O37).
- [x] 4.3 Implementar `GET /app/v1/distribucion/sellos` (`Depends(require_admin)`): lista todos los sellos.
- [x] 4.4 Implementar `PUT /app/v1/distribucion/artistas/{artist_id}/sello` y `PUT /app/v1/distribucion/albumes/{album_id}/sello` (`Depends(require_admin)`): body `{sello_id}`; valida que el artista/álbum exista en `DIM_ARTISTS`/`DIM_ALBUMS` (404 si no) y que el sello exista en `DIM_SELLO_DISCOGRAFICO` (404 si no); ejecuta `ALTER TABLE ... UPDATE sello_id = ... WHERE artist_id/album_id = ...`; registra en `FACT_AUDIT_LOG` (RF-DIS-002, CU-O38).

## 5. Backend: endpoints de licencias (CU-O39/CU-O40)

- [x] 5.1 Implementar `POST /app/v1/distribucion/licencias` (`Depends(require_admin)`): body `sello_id`, `pais_id`, `fecha_inicio`, `fecha_fin` opcional; valida que sello y país existan (404 si no); inserta con `estado='activa'`; registra en `FACT_AUDIT_LOG` (RF-DIS-003, CU-O39).
- [x] 5.2 Implementar `GET /app/v1/distribucion/licencias?sello_id=&pais_id=` (`Depends(require_admin)`): filtra por sello o por país, sin excluir `estado='vencida'` (RF-DIS-004, CU-O40).

## 6. Backend: endpoints de restricciones (CU-O41/CU-O42)

- [x] 6.1 Implementar `POST /app/v1/distribucion/restricciones` (`Depends(require_admin)`): body `fact_id_track`, `pais_id`, `canal_id`, `tipo_restriccion_id`; valida que el track exista en `FACT_TRACKS` (404 si no) y que país/canal/tipo existan (404 si no); inserta en `BRIDGE_RESTRICCION_TRACK` con `activo=1`, `fecha_inicio=now()`; registra en `FACT_AUDIT_LOG` (RF-DIS-005, CU-O41).
- [x] 6.2 Implementar `DELETE /app/v1/distribucion/restricciones/{fact_id_track}/{pais_id}/{canal_id}` (`Depends(require_admin)`): rechaza si no existe una fila activa para esa combinación (404); ejecuta `ALTER TABLE BRIDGE_RESTRICCION_TRACK UPDATE activo = 0 WHERE ... AND activo = 1`; registra en `FACT_AUDIT_LOG` (RF-DIS-006, CU-O42).
- [x] 6.3 Implementar `GET /app/v1/distribucion/restricciones?fact_id_track=` (`Depends(require_admin)`): lista restricciones (activas e inactivas) de un track, para soporte de la UI admin.

## 7. Backend: enforcement en reproducción y consulta de disponibilidad (CU-O43/CU-O44)

- [x] 7.1 Modificar `api/paquetes/biblioteca/router.py::add_historial` (`POST /app/v1/biblioteca/historial/{fact_id}`): antes de `_insert_event(..., "reproduccion")`, resolver `pais_id` del usuario y consultar si existe una restricción activa para `(fact_id, pais_id, canal_id='streaming')` vía `paquetes.distribucion.queries`. Si existe: no insertar en `FACT_ENGAGEMENT_USUARIO`, insertar en `FACT_RESTRICCION_REPRODUCCION` (`fact_id` vía `getrandbits(50)`), y responder `403` con `{"detail": "...", "tipo_restriccion": "..."}` (RF-DIS-007, CU-O43, design.md Decisión 4).
- [x] 7.2 Implementar `GET /app/v1/distribucion/disponibilidad/{fact_id_track}` (`Depends(require_b2c_user)`): resuelve `pais_id` del usuario, consulta si existe una restricción activa para `(fact_id_track, pais_id, canal_id='streaming')` sin insertar nada en `FACT_RESTRICCION_REPRODUCCION`; retorna `{"disponible": bool, "tipo_restriccion": str | null}` (RF-DIS-008, CU-O44).

## 8. Backend: montaje del router

- [x] 8.1 Montar el router de `distribucion` (`prefix="/app/v1/distribucion"`) en `api/main.py` junto al resto de routers (después de `social_router`, mismo patrón de import/`include_router`).

## 9. Frontend: completar el paquete `distribucion` en `frontend/src/packages/distribucion/`

- [x] 9.1 Definir `types.ts` (Sello, Licencia, Restriccion, TipoRestriccion, CanalDistribucion, Pais, y los payloads de request de crear/editar sello, asignar sello, crear licencia, crear/desactivar restricción, y la respuesta de disponibilidad), reemplazando el placeholder actual del stub.
- [x] 9.2 Implementar `api/distribucion.api.ts` con llamadas tipadas a `/app/v1/distribucion/*`, mismo patrón que `packages/social/api/social.api.ts`. Incluye además `paises()`/`canales()`/`tiposRestriccion()` — catálogos de solo lectura no contemplados originalmente aquí, necesarios para poblar los `<select>` de los formularios (endpoints `GET /paises`, `/canales`, `/tipos-restriccion` agregados al backend, ver sección 4).
- [x] 9.3 **Desviación deliberada:** en vez de 3 páginas separadas (9.3/9.4/9.5), se implementó una sola `pages/DistribucionAdminPage.tsx` con pestañas (Sellos/Licencias/Restricciones), cada una en su propio componente bajo `components/` (`SellosTab.tsx`, `LicenciasTab.tsx`, `RestriccionesTab.tsx`). Motivo: `SeguridadShell` usa un ítem de barra lateral por capability (no submenús), y 3 rutas hijas habrían roto ese patrón sin aportar valor — la información sigue completa, solo agrupada. `SellosTab.tsx`: listado de sellos, alta/edición inline (RF-DIS-001), y asignación de sello a artista/álbum por ID (RF-DIS-002).
- [x] 9.4 `LicenciasTab.tsx`: alta de licencia (sello + país + vigencia) y tabla filtrable por sello/país incluyendo vencidas (RF-DIS-003/004).
- [x] 9.5 `RestriccionesTab.tsx`: búsqueda de restricciones por `fact_id_track`, alta de restricción por país/canal/tipo, y tabla con acción desactivar (RF-DIS-005/006), mismo patrón de tabla+acción que `ModeracionSocialPage.tsx` de `social`.
- [x] 9.6 Expuesto desde `index.ts`: `DistribucionAdminPage`, `DisponibilidadPage`, `distribucionApi` y los tipos — nada de `components/`/`api/` interno, regla de aislamiento respetada.
- [x] 9.7 Wiring de rutas: `/seguridad/distribucion` (`DistribucionAdminPage`) bajo `SeguridadShell`, con entrada "Distribución" en la barra lateral admin — un solo path por el cambio de diseño de la tarea 9.3. RF-DIS-008 (consulta de disponibilidad, B2C) expuesta en `/distribucion/disponibilidad` bajo `AppShell`, con entrada "Disponibilidad" en la nav principal — no estaba en el plan de rutas original porque tasks.md no había separado explícitamente dónde vivía cada RF; se resolvió aquí como página mínima propia (ver 9.5 de contexto en README) porque `catalogo` todavía no tiene una vista de detalle de track real en React, mismo criterio ya aplicado en `social` para `ArtistaSocialPage`/`TrackSocialPage`.
- [x] 9.8 **Desviación deliberada, confirmada por hallazgo durante la implementación:** el "reproductor persistente" que realmente llama a `POST /app/v1/biblioteca/historial/{fact_id}` vive en el frontend vanilla (`app/js/history.js`, usado por la app real en el puerto 8081), no en `packages/catalogo` de React (que no tiene esa integración todavía — no hay ningún código en `frontend/src/packages/catalogo` que llame a `/biblioteca/historial`). Se optó por no tocar el componente inexistente y en su lugar aprovechar que `app/js/api.js::apiFetch` ya muestra un toast para cualquier `403` de la API vía `err.detail` (con tal de que sea texto plano, no un objeto) — se ajustó `biblioteca/router.py` para enviar `detail` como string con el `tipo_restriccion` embebido, sin tocar ningún archivo de `app/js/`. El bloqueo es visible en la app real sin trabajo adicional de frontend.
- [x] 9.9 Actualizado el README del paquete quitando la marca de "stub" y corrigiendo la descripción. Documenta la brecha conocida de país no normalizado (design.md, Decisión 5), la brecha de sesión autenticada en React heredada de `facturacion`/`creadores`/`social`, y las dos desviaciones de esta sección (9.3 y 9.8).

## 10. Verificación end-to-end

- [x] 10.0 Creados 2 usuarios de prueba directo vía `pb_client.crear_usuario` (bypass deliberado del guard `ROLES_AUTO_REGISTRABLES` de `/auth/registro`, que no permite autoregistro de `admin` — mismo mecanismo con el que ya existían `social.admin.test@tracklytics.dev` etc. en la base): `distribucion.admin.test@tracklytics.dev` (rol admin) y `distribucion.b2c.test@tracklytics.dev` (rol user, `pais="Ecuador"`, que calca exacto contra la fila sembrada en `DIM_PAIS`). Login vía el endpoint real `/auth/login` para obtener tokens reales (auto-backfill de `DIM_USUARIO`/`FACT_PERMISO_USUARIO` confirmado en la respuesta: `rol: admin` / `rol: user, pais: Ecuador`). Se agregó además un tercer usuario `distribucion.failopen.test@tracklytics.dev` con `pais="Narnia"` (deliberadamente no reconocible) para probar explícitamente el fail-open, no contemplado originalmente en esta tarea pero pedido explícitamente antes de ejecutar la verificación.
- [x] 10.1 Verificado: `POST /distribucion/sellos` → `201 {"sello_id":5,...}`; `PUT /distribucion/sellos/5` → `200`; `GET /distribucion/sellos` refleja el nombre editado.
- [x] 10.2 Verificado: `PUT /distribucion/artistas/1/sello` y `PUT /distribucion/albumes/1/sello` → `200`; `SELECT sello_id FROM DIM_ARTISTS/DIM_ALBUMS WHERE id=1` → `5` en ambos. Bonus: `GET /app/v1/artists/1` (capability `catalogo`) confirma que `record_label` en la respuesta ya se resuelve vía el `JOIN` nuevo, no del campo eliminado.
- [x] 10.3 Verificado: licencia con `fecha_fin` (`201 licencia_id=1`) y sin `fecha_fin` (`201 licencia_id=2`, queda `fecha_fin: null`). `licencia_id=1` marcada manualmente `estado='vencida'` vía `ALTER ... UPDATE` (no hay endpoint de transición de estado — es coherente con el diseño, nada la expone); `GET /licencias?sello_id=5` y `GET /licencias?pais_id=1` devuelven la vencida junto a la activa, sin excluirla.
- [x] 10.4 Verificado: restricción creada en track 163609 (país Ecuador=1, canal streaming=1, tipo geo_bloqueado=3). `POST /biblioteca/historial/163609` con el usuario Ecuador → `403 {"detail":"Este track no está disponible en tu país (geo_bloqueado)"}`; fila confirmada en `FACT_RESTRICCION_REPRODUCCION` con `usuario_id`/`fact_id_track`/`pais_id`/`tipo_restriccion_id` correctos.
- [x] 10.5 Verificado con un track `source_type='real'` (108637): `POST /biblioteca/historial/108637` → `200`, y aparece en `GET /biblioteca/historial`. Nota no contemplada originalmente: el primer intento con un track `source_type='synthetic'` (184626) devolvió `200` y el evento se escribió correctamente en `FACT_ENGAGEMENT_USUARIO` (confirmado por consulta directa), pero no apareció en la respuesta de `/historial` — causa raíz identificada: `HISTORIAL_RECIENTE` (`biblioteca/queries.py`, preexistente, no tocado en este cambio) hace `JOIN` contra una subconsulta de género filtrada a `source_type != 'synthetic'`, así que excluye de la vista cualquier track sin una fila de género "real" asociada. No es un defecto de `distribucion` ni se modifica aquí — documentado como hallazgo para una futura capability que toque `biblioteca`.
- [x] 10.6 Verificado: `DELETE /distribucion/restricciones/163609/1/1` → `200`; `BRIDGE_RESTRICCION_TRACK` queda con `activo=0` (fila conservada, no eliminada); el mismo `POST /biblioteca/historial/163609` ahora responde `200`.
- [x] 10.7 Verificado: restricción recreada en 163609 (tipo `no_disponible`); `GET /distribucion/disponibilidad/163609` → `{"disponible":false,"tipo_restriccion":"no_disponible"}`; `GET /distribucion/disponibilidad/108637` (sin restricción) → `{"disponible":true,"tipo_restriccion":null}`; conteo de `FACT_RESTRICCION_REPRODUCCION` idéntico antes y después de ambas llamadas (1 fila, sin cambios) — confirma que la consulta es de solo lectura.
- [x] **Verificación adicional, pedida explícitamente antes de ejecutar el resto: fail-open con país no reconocido.** Usuario con `pais="Narnia"` (sin match en `DIM_PAIS`): `POST /biblioteca/historial/163609` (con restricción activa) → `200` (no bloquea); `GET /distribucion/disponibilidad/163609` → `{"disponible":true,"tipo_restriccion":null}`. Confirma design.md, Decisión 5, exactamente como se documentó.
- [x] 10.8 Verificado: `DESCRIBE TABLE DIM_ARTISTS`/`DIM_ALBUMS` ya no listan `record_label`/`label`, sí listan `sello_id`. `catalogo` (`/app/v1/artists/1`) confirmado funcionando con el nombre resuelto vía join (10.2). `partners/queries.py` no se re-verificó con una llamada HTTP real (requiere una API key de partner, fuera de alcance provisionar en esta ronda) — se verificó por identidad de patrón: usa el mismo `JOIN DIM_SELLO_DISCOGRAFICO` recién confirmado funcionando en `catalogo`.
